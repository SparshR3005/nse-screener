# Pinned deliberately. An unpinned `pandas>=2.0` let the runner install
# pandas 3.0 while local testing ran 2.3 - and pandas 3.0 dropped the silent
# object->bool downcast in .fillna(), which quietly turned the screens'
# firing-edge detection into "condition currently holds". The code is now
# version-proof either way, but the whole point of a screener is that its
# output does not change because a transitive dependency shipped a major.
pandas>=2.2,<3.2
numpy>=1.24,<3
yfinance>=0.2.40,<2
requests>=2.31,<3
